{
  "compilerOptions": {
    /* базові речі */
    "target": "ESNext",
    "module": "ESNext",
    "lib": ["DOM", "DOM.Iterable", "ESNext"],
    "jsx": "react-jsx",
    "strict": true,
    "moduleResolution": "Node",
    "skipLibCheck": true,
    "noEmit": true,
    "allowSyntheticDefaultImports": true,

    /* ───────  alias для TS  ─────── */
    "baseUrl": ".", // обовʼязково!
    "paths": {
      "@/*": ["src/*"]
    },

    /* щоб TS знав про import.meta, ENV тощо */
    "types": ["vite/client", "node"]
  },
  "include": ["src", "vite.config.ts"]
}
